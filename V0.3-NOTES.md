# v0.3 completion notes

- The web dashboard and API run as one process.
- The Discord slash-command companion runs as a separate process using `lexus-hub bot`.
- Both processes share the same `.data` directory.
- Private remote access is expected to be provided by the host network layer, such as Tailscale.
- Do not expose the dashboard directly to the public internet without adding application authentication and TLS.

# v0.3 deployment

The dashboard is served on port 8000 and the Discord companion runs with `lexus-hub bot`.

Both processes should use the same private `.env` and the same `.data` directory. Keep both files/directories outside source control.

For remote access, use a private network path to the Raspberry Pi instead of public port forwarding.
